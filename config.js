// config.js  –  Cấu hình chung cho tất cả các mẫu báo cáo
// URL thật của Google Apps Script được ẩn trong Vercel (biến môi trường GAS_URL).
// Các trang HTML chỉ gọi /api/proxy — không bao giờ biết URL GAS thật.

const GAS_URL = "/api/proxy";
